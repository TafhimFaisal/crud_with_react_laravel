import React from 'react';

const LoginForm = () => {
    return (
        <React.Fragment>
            <h2>not a new user</h2>
            <h4>Login to your account</h4>
        </React.Fragment>
    );
};

export default LoginForm;
