import React from 'react';

const NotesCollection = () => {
    return (
        <React.Fragment>
            <h2>Notes Collection</h2>
        </React.Fragment>
    );
};

export default NotesCollection;
